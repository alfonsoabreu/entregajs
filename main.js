let afili = prompt("Esta registrado, ingrese si o no.").toLocaleUpperCase();
let libf = 50;
let liba = 50;
let libc = 100;

if (afili = "si"){
    let NlibF = parseint(prompt("cantidad de libros de ficcion "));
    let NlibA = parseint(prompt("cantidad de libros de accion"));
    let NlibC = packagerseint(prompt("cantidad de libros comedia"));
    const cuenta = (`El monto total es: ${libf*Nlibrosficcion} + ${liba*Nlibrosaccion} + ${libc*Nlibroscomedia}`)
}else{
    alert("debe registrarse antes de ingresar");
}
